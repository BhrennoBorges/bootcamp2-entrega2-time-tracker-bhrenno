name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # Se package-lock.json estiver versionado, npm ci é preferível.
      - name: Instalar dependências
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Instalar Playwright (Chromium + deps)
        run: npx playwright install --with-deps chromium

      - name: Build da extensão (gera dist/ + zip)
        run: npm run build

      # 👇 Rodar Chromium com extensão sob um X virtual
      - name: Testes E2E (Chromium com extensão via Xvfb)
        run: xvfb-run -a npm run test:e2e

      - name: Publicar relatório do Playwright (HTML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 14

      - name: Publicar pacote da extensão (ZIP)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: extension-zip
          path: dist/extension.zip
          retention-days: 30
